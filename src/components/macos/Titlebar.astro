---
interface Props {
	active?: boolean;
	expandable?: boolean;
	title?: string;
}

const { active = true, expandable = true, title = "" } = Astro.props;
---

<div class="relative h-7 w-full border-b border-black">
	<!-- bg lines -->
	<div
		class="absolute left-0 top-0 h-full w-full flex-col justify-center gap-0.5 bg-white"
		class:list={[{ hidden: !active }, { flex: active }]}
	>
		<div class="w-full border-b border-black"></div>
		<div class="w-full border-b border-black"></div>
		<div class="w-full border-b border-black"></div>
		<div class="w-full border-b border-black"></div>
		<div class="w-full border-b border-black"></div>
		<div class="w-full border-b border-black"></div>
	</div>

	<div
		class="relative h-full w-full flex flex-row items-center justify-between px-4"
	>
		<!-- close btn -->
		<button
			class="size-4 border border-black bg-white"
			class:list={[{ hidden: !active }, { block: active }]}></button>

		<!-- window title -->
		<div class="inline-flex items-center justify-start gap-2.5 bg-white px-2">
			<div
				class="text-center text-xl text-black font-medium leading-none font-['ChicagoFLF']"
			>
				{title}
			</div>
		</div>

		<!-- maximize btn -->
		<button
			class="relative size-4 border border-black bg-white"
			class:list={[
				{ hidden: !active || !expandable },
				{ block: active && expandable },
			]}
		>
			<div
				class="absolute left-0 top-0 h-2.5 w-2.5 border-b border-r border-black"
			>
			</div>
		</button>
	</div>
</div>
